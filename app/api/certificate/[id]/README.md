# app/api/certificate/[id]/

- `GET` — Genera y retorna el certificado de un colaborador para una capacitación específica en formato PDF. Valida que el colaborador haya aprobado el quiz antes de emitir el certificado. El certificado incluye nombre del colaborador, nombre de la capacitación, fecha de emisión y código único de verificación.
